{*
 * @var $article
 *}
{block content}
<h3>Obnovení hesla</h3>
{snippet flashes}
	<div n:foreach="$flashes as $flash" class="alert alert-{$flash->type}" role="alert">
		{$flash->message|noescape}
	</div>
{/snippet}
{snippet form}
	<form n:name="chooseNewPasswordForm" n:if="!count($flashes)" class="ajax">
		<div n:foreach="$form->getErrors() as $error" class="alert alert-danger" role="alert">
			{$error|noescape}
		</div>
		<p>Pro dokončení obnovy hesla zadejte své nové heslo do polí níže.</p>
		<div class="form-group">
			<label>Heslo <a href="#"><i class="glyphicon glyphicon-info-sign" data-trigger="hover" title="Složitost hesla" data-toggle="popover" data-placement="top" data-content="Vámi zvolené heslo může mít přímý dopad na bezpečnost Vašeho účtu. Ve Vašem účtu se nachází potenciálně citlivé údaje jako je telefon, e-mail, nebo adresa apod. Doporučujeme zvolit heslo které bude min. 8 znaků dlouhé a bude složené z malých / velkých písmen, čísel a ideálně i speciálních znaků."></i></a> <span class="required">*</span></label>
			<input n:name="password" type="password" class="form-control">
		</div>
		<div class="form-group">
			<label>Heslo znovu<span class="required">*</span></label>
			<input n:name="passwordRe" type="password" class="form-control">
		</div>
		<input type="hidden" n:name="address">
		<input type="hidden" n:name="hash">
		<input n:name="submit" type="submit" class="btn btn-primary btn-lg btn-block" value="Resetovat heslo">
	</form>
{/snippet}